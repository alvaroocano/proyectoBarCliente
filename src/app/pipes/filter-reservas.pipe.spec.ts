import { FilterReservasPipe } from './filter-reservas.pipe';

describe('FilterReservasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReservasPipe();
    expect(pipe).toBeTruthy();
  });
});
