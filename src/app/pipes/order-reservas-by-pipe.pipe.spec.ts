import { OrderReservasByPipePipe } from './order-reservas-by-pipe.pipe';

describe('OrderReservasByPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderReservasByPipePipe();
    expect(pipe).toBeTruthy();
  });
});
